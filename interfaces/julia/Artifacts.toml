# This file is intentionally empty in source checkouts until release tarballs are bound.
#
# The expected artifact name is "hypredrive_mpi_trampoline". The historical key
# now covers the platform-specific HYPREDRV Julia bridge: MPItrampoline is used
# on Linux/macOS and Microsoft MPI on Windows. Release tooling should bind the
# platform tarballs with `interfaces/julia/binary/bind_artifact.jl`.
